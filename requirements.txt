procrastinate[psycopg]==3.9.0
psycopg[binary]==3.*
psycopg-pool==3.*
fastapi==0.115.*
uvicorn[standard]==0.32.*
google-cloud-storage==2.18.*
google-cloud-bigquery==3.25.*
httpx==0.27.*
pytest==8.*
pytest-asyncio==0.24.*

# OpenTelemetry → SigNoz (foundation). Pins are a matched set: SDK/exporter
# at 1.44.0 with instrumentation at 0.65b0 (same release train as
# opentelemetry-semantic-conventions==0.65b0). Do not mix major/minor across
# these lines — OTel Python packages are version-locked and mismatches raise
# confusing ImportErrors at runtime.
opentelemetry-sdk==1.44.0
opentelemetry-exporter-otlp-proto-grpc==1.44.0
opentelemetry-instrumentation-fastapi==0.65b0
opentelemetry-instrumentation-httpx==0.65b0
opentelemetry-instrumentation-psycopg==0.65b0
opentelemetry-instrumentation-logging==0.65b0
